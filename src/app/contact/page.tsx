import React from 'react';
import ContactClient from './ContactClient';

export const metadata = {
    title: 'Contact Us | X-Group Catering & Convention Reservations',
    description: 'Planning an event? Reach out to our expert team for venue viewings, booking inquiries, and customized catering quotes in Dhaka.',
    keywords: ["contact x-group", "catering reservation dhaka", "event planning inquiry"],
};

export default function ContactPage() {
    return <ContactClient />;
}
